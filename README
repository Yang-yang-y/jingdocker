#jingdocker
启动:
     1.将根目录.env.example更名为.env
     2.执行docker-compoese xxx.yml up/docker-compoese -f xxx.yml -f xxx1.yml up
配置:
    SCRIPT为jingweb前端启动项
     1.SCRIPT=dev 通过nodejs暴露端口，访问端口NODEJS_CLIENT_PORT=8080
     2.SCRIPT=test 通过nginx暴露端口，访问端口NGINX_JINGWEB_HOST_HTTP_PORT=4080